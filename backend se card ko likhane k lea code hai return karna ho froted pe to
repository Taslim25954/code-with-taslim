 return (
    <div style={container}>
      {profiles.map((p) => (
        <div key={p.id} style={card}>
          <img src={p.coverImage} alt="" style={image} />

          <div style={content}>
            <h3>{p.fullName}</h3>
            <p style={{ opacity: 0.7 }}>{p.title}</p>

            <div style={stats}>
              <span>📌 {p.postCount}</span>
              <span>👁 {p.viewCount}</span>
            </div>

            <p style={{ fontSize: "13px", opacity: 0.8 }}>
              {p.description}
            </p>

            <button style={btn}>
              {p.followed ? "Following" : "Follow"}
            </button>
          </div>
        </div>
      ))}
    </div>
  );
};

export default Card;
